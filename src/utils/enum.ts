export enum userRole {
  ADMIN = 'admin',
  User = 'user',
  Agent = 'agent',
}

export enum AccountStatus {
  Pending = 'pending',
  Active = 'active',
  Suspended = 'suspended',
  Banned = 'banned',
}

export enum AgentStatus {
  ACTIVE = 'active',
  SUSPENDED = 'suspended',
  BANNED = 'banned',
}
