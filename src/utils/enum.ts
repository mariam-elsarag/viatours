export enum userRole {
  ADMIN = 'admin',
  User = 'user',
  Agent = 'agent',
}

export enum AccountStatus {
  Pending = 'pending',
  Active = 'active',
  Suspended = 'suspended',
  Banned = 'banned',
}

export enum AgentStatus {
  PENDING = 'pending',
  ACTIVE = 'active',
  SUSPENDED = 'suspended',
  REJECTED = 'rejected',
}
